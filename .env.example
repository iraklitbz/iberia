# Frontend URL
NUXT_PUBLIC_SITE_URL=https://iberiainfo.org

# Strapi
STRAPI_URL=http://localhost:1337
STRAPI_TOKEN=tu_token_de_strapi_solo_servidor
# Set true only temporarily if you need to read old forum posts from Entrada during migration.
FORUM_LEGACY_FALLBACK=false

# Email de contacto
RESEND_API_KEY=tu_resend_api_key
CONTACT_TO=email_receptor@gmail.com

# Webhook revalidación
REVALIDATE_SECRET=un_secret_largo_y_random

# Cloudflare cache purge
CLOUDFLARE_API_TOKEN=tu_cloudflare_api_token
CLOUDFLARE_ZONE_ID=tu_zone_id
# Si llega webhook sin body usa estos paths (se convierten a URLs con NUXT_PUBLIC_SITE_URL)
CLOUDFLARE_PURGE_PATHS=/*
# true = purga total solo cuando el webhook llega sin paths/urls
CLOUDFLARE_PURGE_EVERYTHING=false
